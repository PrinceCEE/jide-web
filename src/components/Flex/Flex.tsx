import { FC } from "react";
import { FlexType } from "src/types";

export const Flex: FC<FlexType> = () => {
  return <div></div>;
};

export const Column = () => {
  return <Flex />;
};

export const Row = () => {
  return <Flex />;
};
