import { FC } from "react";
import { ButtonType } from "src/types";

export const Button: FC<ButtonType> = () => {
  return <button></button>;
};

export const TextButton: FC<ButtonType> = () => {
  return <button></button>;
};
