import { FC } from "react";
import { TextInputType } from "src/types";

export const TextInput: FC<TextInputType> = () => {
  return <input />;
};

export const PasswordInput = () => {
  return <TextInput />;
};

export const CheckBoxInput = () => {
  return <input />;
};

export const RadioInput = () => {
  return <input />;
};

export const FileUploadInput = () => {
  return <input />;
};
